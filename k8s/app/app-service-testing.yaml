apiVersion: v1
kind: Service
metadata:
  name: poc-hexagonal-arch
  namespace: default
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: nginx
