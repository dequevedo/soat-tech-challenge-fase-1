apiVersion: v1
kind: Service
metadata:
  name: poc-hexagonal-arch
spec:
  selector:
    app: poc-hexagonal-arch
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort