apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-pvc
spec:
  accessModes:
    - ReadWriteOnce #is this going to allow Many pods to a single DB? or should I try ReadWriteMany?
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual